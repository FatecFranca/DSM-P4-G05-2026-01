name: CD

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    name: Deploy backend (Render)
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        if: env.RENDER_BACKEND_DEPLOY_HOOK_URL != ''
        env:
          RENDER_BACKEND_DEPLOY_HOOK_URL: ${{ secrets.RENDER_BACKEND_DEPLOY_HOOK_URL }}
        run: curl -sS -X POST "$RENDER_BACKEND_DEPLOY_HOOK_URL"

  deploy-front:
    name: Deploy frontend (build + Render)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: front/package-lock.json
      - run: npm ci
        working-directory: front
      - run: npm run build
        working-directory: front
        env:
          CI: "true"
      - name: Trigger Render static deploy hook
        if: env.RENDER_FRONT_DEPLOY_HOOK_URL != ''
        env:
          RENDER_FRONT_DEPLOY_HOOK_URL: ${{ secrets.RENDER_FRONT_DEPLOY_HOOK_URL }}
        run: curl -sS -X POST "$RENDER_FRONT_DEPLOY_HOOK_URL"

  publish-mobile:
    name: Publish mobile (EAS Update)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile/GymRadar
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/GymRadar/package-lock.json
      - run: npm ci
      - name: Push an OTA update via EAS
        if: env.EXPO_TOKEN != ''
        continue-on-error: true
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: npx eas-cli update --auto --non-interactive
